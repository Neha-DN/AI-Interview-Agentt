{
  "name": "ai-interview-agent",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && rm -rf dist .output && cp -r frontend/.output .output && cp -r frontend/.output/public dist",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint"
  }
}
